<h1>details for user '<%= @user.username %>'</h1>

<p>album reputation: <%= @user.album_reputation %></p>
<p>comment reputation: <%= @user.comment_reputation %></p>
<p>total reputation: <%= @user.total_reputation %></p>

<h2>uploaded albums</h2>

<div style="max-width:600px">
  <% @user.albums.each do |album| %>
    <a href="<%= album_url(album) %>">
      <img src="<%= album.images.first.file.url(:small) %>">
    </a>
  <% end %>
</div>

<% if current_user && current_user.id == @user.id %>

  <p>username: <%= @user.username %></p>
  <p>email address: <%= @user.email %></p>
  <p>password: TOP SECRET but maybe you can reset it later</p>
  <a href="<%= edit_user_url(@user) %>">update your account details</a>
<% end %>