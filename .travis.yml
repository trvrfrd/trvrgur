# Travis will use version specified in .ruby-version
language: ruby
cache: bundler
before_script:
  - psql -c 'CREATE USER trevor;' -U postgres
  - bundle exec rake db:setup
  - bundle exec rake db:test:prepare
